package com.company;

import java.util.ArrayList;
import java.util.Collections;

public class Data_task1 {

    public static void task1(){


 /*       ArrayList<String> strArray= new ArrayList<String>();
        strArray.add("Computer");
        strArray.add("Plate");
        strArray.add("Chair");
        strArray.add("Girl");
        strArray.add("Boy");
        strArray.add("Cat");
        strArray.add("Dog");
        strArray.add("Shirt");
        strArray.add("Determination");

        System.out.println("Unsorted list " + strArray);
        Collections.sort(strArray);
        System.out.println("Sorted list in ascending order by the first letter " + strArray);



        int counter = 0;
        int s = counter;
        int e = counter;
        int w = counter;
        int c = counter;
        boolean te = true;


        for (String value : strArray) {
            if (value.startsWith("C")) {
                s++;
            }
        }
        System.out.println(s + " values start with C");


        for (String value : strArray) {
            if (value.endsWith("e")) {
                e++;
            }
        }
        System.out.println(e + " values ends with e");


        for (String value : strArray) {
            if (value.length() == 5) {
                w++;
            }
        }
        System.out.println(w + " values are with length 5");

        for (String value : strArray) {
            if (value.contains("e")) {
                c++;
            }
        }
        System.out.println(c + " values consist of the character “e”");

        for (String value : strArray) {
            if (value.contains("te")) {
                te = true;

            }
        }
        System.out.println("There are element which consists of the subString “te”: " + te);

        System.out.println();
*/


    }
}
